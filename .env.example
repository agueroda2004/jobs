# Server-side auth credentials (compared in api/auth.ts, never shipped to the client)
AUTH_USERNAME="your-username"
AUTH_PASSWORD="your-password"

# Neon Postgres connection string (used later for the data migration)
DATABASE_URL="postgresql://user:password@host/db"